@model IEnumerable<TimeTracker.Models.WorkingCardViewModel>

@{
    ViewBag.Title = "ShowAll";
}

<h2>ShowAll</h2>

@(Html.Kendo().Grid(Model)
    .Name("ActiveUsers")
    .Columns(columns =>
    {
        columns.Bound(p => p.TaskTitle);
        columns.Bound(p => p.StartDate);
        columns.Bound(p => p.WorkingHours);
        columns.Bound(p => p.Description);
        columns.Template(o => Html.ActionLink("Edit", "Edit", "WorkingCard", new { id = o.wCardId }, null));
        columns.Template(o => Html.ActionLink("Details", "Details", "WorkingCard", new { id = o.wCardId }, null));
        
    })
    .Sortable()
    .Pageable()
)


@*<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.StartDate)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.WorkingHours)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Description)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.IsFilled)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.StartDate)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.WorkingHours)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Description)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.IsFilled)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
        </td>
    </tr>
}

</table>*@
